# Python deps for Vercel serverless functions under /api.
# The Next.js app itself needs nothing here.
#
# analyze_xlsx.py (legacy, rules-only) + pm_dump.py (mechanical xlsx dump, no AI) need openpyxl.
# Pricing Matrix v2 computes every quote itself (pm-calc.ts) from a normalized rate table — no
# workbook-execution engine is run at quote time, so no heavy numeric deps (numpy/scipy/etc) here.
openpyxl>=3.1
