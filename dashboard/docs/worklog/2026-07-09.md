# Worklog — 2026-07-09

Everything shipped today (all on `main` / Vercel production). 23 commits.

## RFQ engagement — capture, decide, close, audit
- **Reply-To operations@** on personal sends — insurer/client replies route back to the shared Engagement mailbox, not the employee's inbox. (`dc7e696`)
- **Per-line quote decision inside Run Analysis** — RFQ case → Opus lays out each insurer objectively (pros/cons) + a recommendation; figures DB-sourced, never invented. (`db1eba3`)
- **Pre-send quote verification failsafe** — every figure checked 3 ways (verbatim in source, in its cited excerpt, second-model consensus) before recommending; catches 254,000→245,000. Modal with acknowledge gate. (`4c5d2e8`)
- **Multi-select lines in the RFQ wizard** — tick several lines, configure insurers sequentially per line, Send all. (`32d2d6c`)
- **Signature de-dup + preview** on RFQ Review & Send. (`e8273d7`)
- **Outcome simplified to Selected / Not chosen** (was bind/won/lost + premium/date/policy). (`2669d08`)
- **One Nexus case per line of insurance** + standardized title `[RFQ] {insured} — {line}`. (`23af39d`)
- **Pipeline funnel + win/selection-rate analytics** on the RFQ Ops panel. (`ff367f6`, `c00dd1f`)
- **Full RFQ audit trail** — `rfq_events` logs every touchpoint (requested→dispatched→replied→quoted→recommended→selected/not_chosen) with real ids + actor. (`2669d08`)

## Nexus
- **ID foundation** — deterministic entity linking so stakeholders, missing-items, next-steps, citations join on real ids instead of fuzzy text. (`be86379`)
- **Every next-step is draftable** — "Draft in Engagement" on every step, editable recipient when it doesn't auto-resolve; steps are independent. (`0c28ab2`)
- **Documents index** — every attachment grouped by who sent it (Client / each insurer), tracked by id, read/pending status. (`b885a0a`)
- **UX pass** — scenarios, next steps, stakeholders table, link-threads, chat label. (`646561d`)

## Analytics & learning
- **AI Usage** now tracks Opus + Gemini by provider/model (Opus was never logged); two views (product area / model); correct Flash pricing $0.30/$2.50. (`3c61e35`, `5962f1a`)
- **Eval** surfaces RFQ + Nexus eval surfaces grouped by product area; real AI-draft-vs-human-sent signal on RFQ. (`a00d406`, `f5f705e`)

## Engagement & contacts
- RFQ/reply → Engagement threads: auto-CC ops, instant RFQ thread, clearer sender/signature. (`7e28969`)
- TRS name for internal-only threads; RFQ 'Done' state. (`7d71b9d`)
- Bulk CSV contact import with duplicate/invalid review. (`ed6f023`)

## Settings & housekeeping
- Settings top-tab navigation + client recommendation template. (`0881294`)
- Untrack `tsconfig.tsbuildinfo`. (`1fa644f`)
- Dogfood unit tests for quote-verify + signature-dedup (suite 52 → 63). (`2184695`)

## Migrations run in Supabase today
`20260709_rfq_quotes` · `20260708_chat_*` (earlier) · `20260709_rfq_outcomes` · `20260709_ai_usage_provider` · `20260710_rfq_events`.
